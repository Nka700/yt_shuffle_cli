# Bash → Python 移行理由

## 背景

本プロジェクトは2つのBashスクリプト（`lsytmplylst`, `mpvy`）で構成されていた。

使い方:
```bash
# プレイリストを変数で取得
source lsytmplylst

# 実行
mpvy $プレイリスト変数
```

リファクタリングにあたり、Pythonへ移行する判断を行った。その理由を以下に記す。

## 移行理由

### 1. 外部依存の削減

| 機能                | Bash版の依存 | Python版の依存                    |
|---------------------|--------------|-----------------------------------|
| YouTube API呼び出し | `curl`       | 標準ライブラリ (`urllib.request`) |
| JSONパース          | `jq`         | 標準ライブラリ (`json`)           |

- `curl` と `jq` はPython標準ライブラリで完全に代替できる。追加インストール不要。
- 削減不可能な依存（`mpv`, `yt-dlp`）はそのまま継続する。これらはPythonでも代替が存在しない。

### 2. エラーハンドリングの改善

- Bashではパイプライン中のエラーが暗黙的に無視されやすい（`curl` 失敗時に `jq` が空入力でエラーになる等）。
- Pythonの例外機構により、APIエラー・ネットワークエラー・不正な入力を明確に捕捉・報告できる。

### 3. API応答処理の堅牢性

- `lsytmplylst` はページネーション未対応で、50件を超えるプレイリストを取得できなかった。
- Pythonでは `nextPageToken` を使ったループ処理を自然に記述でき、全件取得が可能になる。

### 4. 統合CLIとしての一貫性

- 2つの独立スクリプトを1つのCLIツール (`ytm`) に統合することで、使い方が統一される。
- サブコマンド形式 (`ytm playlists`, `ytm play`) により発見性が向上する。
- `source lsytmplylst` という特殊な使い方を `eval $(ytm playlists --export)` に統一し、通常のコマンド実行と同じ形式にできる。

### 5. 保守性

- Bashの文字列処理（`tr`, `sed` の組み合わせ）よりPythonの文字列操作の方が可読性が高い。
- 型がないBashに対し、Pythonは動的型付けながらも構造化されたデータ処理が容易。

## 移行しない要素

- `mpv` と `yt-dlp` は外部コマンドとして引き続き `subprocess` 経由で呼び出す。
  これらはメディア再生・動画処理の専用ツールであり、Pythonで代替する利点がない。

## 依存関係まとめ

### 削減された依存
- `curl` → 不要
- `jq` → 不要

### 継続する依存
- `mpv` — メディアプレーヤー
- `yt-dlp` — YouTube動画/音声処理

### 新規依存
- `Python 3.8+`
